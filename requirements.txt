# Librerie per il database
sqlalchemy>=2.0  # ORM per gestire i database relazionali, come SQLite

# Librerie per la gestione delle date e dei formati
python-dateutil>=2.8  # Per la gestione avanzata delle date e dei fusi orari

# Librerie per la GUI (se usi interfacce grafiche)
PyQt5>=5.15  # Toolkit GUI, richiede PyQt5 Designer per design grafico
# oppure, se preferisci Tkinter (incluso in Python):
# tk  # Non necessita di installazione separata

# Libreria per la gestione dei test
pytest>=7.0  # Per eseguire test su componenti specifici

# Libreria per facilitare la serializzazione dei dati
pydantic>=1.10  # Per validazione e gestione di dati (facoltativa)
